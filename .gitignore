# Godot generated cache / import artifacts (regenerated by `just import` / editor)
.godot/

# Local build + packaging output (see justfile `export` / `package`)
build/
dist/
HEAVENLY-windows-x86_64.zip
HEAVENLY-windows-x86_64.zip.sha256
*.pck
*.pdb

# macOS / Windows / Linux filesystem cruft
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
Desktop.ini
$RECYCLE.BIN/

# Editor / IDE local state
# NOTE: .vscode/launch.json + .vscode/settings.json are intentionally tracked.
.vscode/ipch/
.vscode/.ropeproject/
.idea/
*.swp
*.swo
*~
.vs/
*.user
*.suo

# Logs, temp files, crash dumps
*.log
*.tmp
*.temp
mono_crash.*.json
core
